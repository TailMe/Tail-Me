<nav class='navbar scrolled' id='scroll-toggler'> {{!-- ${this.props.class} --}}
    <div class='wrapper'>
        <div class='navbar-hamburger'>
            <div class='navbar-toggle'> {{!-- ${iconToggled}`} --}}
                <button type='button' class='navbar-toggler'> {{!-- onClick={() => this.toggleMenu()} --}}
                    <span class='navbar-toggler-bar bar1'></span>
                    <span class='navbar-toggler-bar bar2'></span>
                    <span class='navbar-toggler-bar bar3'></span>
                </button>
            </div>
        </div>
        <img src='/public/assets/imgs/logo.png' class='logo' alt='TailMe' />
        <div class='navbar-nav'> {{!-- ${navbarToggled} --}}
            <a href='/signup' class='navbar-nav-item'>Become a Walker</a>
            <a href='/services' class='navbar-nav-item'>Services</a>
            <a href='/about' class='navbar-nav-item'>About</a>
            <a href='/blog' class='navbar-nav-item'>Blog</a>
            <a href='/contact' class='navbar-nav-item'>Contact</a>
        </div>
    </div>
</nav>